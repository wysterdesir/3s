{
  "_note": "Hand-written overrides for tools/transcode.js: their filename slug (or noise-stripped core) -> our exercise id. Emptied when the library was rebuilt from the catalogue. js/exercises.js is now GENERATED from specific source files, and tools/catalogues/clips.json records exactly which file every exercise came from, so there is nothing left to guess — the previous entries all named hand-authored ids that no longer exist. Add a line here only for an exercise clips.json does not cover; entries naming an unknown id are ignored."
}
